package Chapter4;

public class bit4question1_b {
    public static void main(String[] args) {
        int a = 5, b = 4, c = 3;
        float r = (float)(Math.pow(a, 2) + Math.pow(b, 2) + Math.pow(c, 2)) / (a * b * c);
        System.out.println("The output is: "+r);
    }
}
